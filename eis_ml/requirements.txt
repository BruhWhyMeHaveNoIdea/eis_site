# Dependencies for both select_k_best and ml_retrospective algorithms
# This file combines core and ML-specific requirements

# Core numerical and data processing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
python-dateutil>=2.8.2

# Text embedding and NLP
sentence-transformers>=2.2.0
torch>=2.0.0
transformers>=4.30.0

# Russian language processing (optional but recommended)
pymorphy2>=0.9.1
pymorphy2-dicts-ru>=2.4.417127.4579844

# Machine learning
scikit-learn>=1.3.0

# Dimensionality reduction and clustering
umap-learn>=0.5.0
hdbscan>=0.8.0

# Similarity search
faiss-cpu>=1.7.0  # Use faiss-gpu for GPU acceleration

# Visualization (optional)
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars
tqdm>=4.65.0

# Performance and caching
joblib>=1.2.0

# Development and testing
pytest>=7.3.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=0.990

# Note: Some packages may have system dependencies
# For pymorphy2 on Windows, you might need to install Microsoft Visual C++ Build Tools
# For FAISS, you may need to install via conda or use faiss-cpu (pip)